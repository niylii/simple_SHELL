#include "main.h"

/**
 * main - Entry point of the program.
 *
 * This function calls the simple_shell10 function to execute the shell.
 * It returns 0 upon successful completion.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	simple_shell10();
	return (0);
}
